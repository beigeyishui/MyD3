/*

    enqueue(d) 入队， 将数据存入队列中
    dequeue() 出队, 将数据从队列中取出
 */

function Queue() {
    var q = {},
        val = [],   //数据存放数组
        head = 0,   // 队首
        tail = 0;   // 队尾

    // 入队
    q.enqueue = function(d) {

    };
    // 出队
    e.dequeue = function() {

    };
    return q;
}